# Development dependencies: everything in prod plus local-only tooling.
# Use this for local development and CI:  pip install -r requirements-dev.txt
-r requirements.txt

# Local LLM provider (dev default). Prod uses the hosted OpenAI-compatible
# client over httpx, so ollama isn't needed in the deployed bundle.
ollama==0.3.3

# Vector store for RAG (Sprint 3). Not imported yet, so excluded from the prod
# bundle for now; revisit when RAG ships to deployment.
chromadb==0.5.5

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
